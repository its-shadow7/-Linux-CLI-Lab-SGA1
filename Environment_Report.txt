Environment Report



1. Username and Groups: 

Commands: whoami, groups 



Observation: I executed the whoami command to display my current username. Then, I used the groups command to view all the groups my account belongs to. The output showed my username and the list of groups associated with my user account. 



2. Current shell:

Command: echo $SHELL

Observation: I used echo $SHELL to print the environment variable that stores my current shell. The output confirmed I am using the bash shell.



3. Current working directory:

Command: pwd

Observation: I ran pwd to verify my current location in the file system. It output the absolute path of my active workspace, showing I am inside the Q1 folder.



4. List of files:

Command: ls -la

Observation: I used ls -la to list all files and directories in my workspace, including hidden files. I observed the permissions, ownership, and timestamps for each item.



5. Network connectivity:

Command: ping -c 4 google.com

Observation: I executed a ping command limited to 4 packets to test network connectivity. The output showed successful packet transmission and reception, confirming internet access.